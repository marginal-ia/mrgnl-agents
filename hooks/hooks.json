{
  "hooks": [
    {
      "event": "PostToolUse",
      "matcher": {
        "tool": ["Write", "Edit"],
        "file_pattern": "*.proto"
      },
      "type": "prompt",
      "prompt": "You just edited a .proto file. Remember to run `buf lint` and `buf breaking --against .git#branch=main` before committing. After validation, run `buf generate` to regenerate Go code and commit generated files alongside proto changes."
    },
    {
      "event": "PostToolUse",
      "matcher": {
        "tool": ["Write", "Edit"],
        "file_pattern": "**/queries/*.sql"
      },
      "type": "prompt",
      "prompt": "You just edited a sqlc query file. Remember to run `sqlc generate` to regenerate the Go code before committing."
    }
  ]
}
