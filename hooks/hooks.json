{
  "description": "Reminders for buf and sqlc workflows after editing proto or query files",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if the file just edited is a .proto file. If so, remind the user to run `buf lint` and `buf breaking --against .git#branch=main` before committing, then `buf generate` to regenerate Go code. If the file is a .sql file under a queries/ directory, remind to run `sqlc generate`."
          }
        ]
      }
    ]
  }
}
